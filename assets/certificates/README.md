# Certificates

Put certificate images/PDFs here (e.g. `agentic-ai-deeplearningai.pdf`) and link them from the
`href="#"` placeholders in `index.html`'s `#certifications` section — or link directly to the public
verification URL (Coursera, Credly, edX, etc.) instead of hosting a file at all.
