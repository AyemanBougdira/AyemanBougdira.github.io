# Books

Put book cover images here (e.g. `atomic-habits.jpg`) and reference them as a background/img inside the
`.book-cover` placeholders in `index.html`'s `#books` section. Duplicate a `.book-card` block for each
new entry and set its title, author and status (`read` / `reading`).
